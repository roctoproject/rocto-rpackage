# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

dist: trusty
language: R
sudo: true
cache: packages
r:
  - devel
before_script:
  - Rscript -e 'if(!require(roxygen2)){install.packages("roxygen2")};devtools::test()'
